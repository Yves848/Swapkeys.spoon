# Historique — 2026-07-21

> Journal des demandes de la journée. Les entrées « Demande » sont consignées automatiquement (hook) ; les sections « Réalisé » sont ajoutées par le skill `historiser`.

## 07:15:37 — Demande

> oui, retire window-snap.lua

## 07:20:55 — Demande

> Je teste la navigation après un reboot pour confirmer

## 07:25:41 — Demande

> reboot fait, c'est ok

## 07:30 — Réalisé : fix course au login + retrait du fallback window-snap

**Demande :** navigation en collision après login ; retirer `window-snap.lua` ; valider
après reboot.

**Actions :**
- Diagnostic : au login, Hammerspoon démarrait **avant** que le service yabai réponde → le
  garde `yabaiUp` (test `query --displays`) échouait → chargement du fallback
  `window-snap`, dont les raccourcis entraient en collision avec la navigation yabai.
- Fix `init.lua` : gate désormais sur la **seule présence du binaire** yabai (plus de test
  de réponse du service) → plus de course au login.
- Retrait du fallback : `require("window-snap")` supprimé et fichier
  `~/.hammerspoon/window-snap.lua` supprimé (couvert par `WindowSnap.spoon`).
- **Validé après reboot** : navigation OK d'emblée.

**Fichiers :** `~/.hammerspoon/init.lua`, `~/.hammerspoon/window-snap.lua` (supprimé).

**Commits :** `ddbab09` (fix course au login), `8df79a6` (retrait fallback) — poussés (GitHub).

## 07:51:51 — Demande

> oui, commit et push

## 07:54:21 — Demande

> petit détail : quand on "maximise" les fenêtres, on ne voit plus sketchybar

## 08:10 — Réalisé : réserver le haut pour SketchyBar (fenêtres maximisées)

**Demande :** une fenêtre « maximisée » passait sous SketchyBar (invisible).

**Actions :**
- Cause : barre native masquée → `hs.screen:frame()` ne réserve plus le haut ; le
  placement remplissait tout l'écran, sous l'overlay SketchyBar (`topmost=off`).
- `WindowSnap.spoon` : champ `topInset` + calcul de la zone utile réservant
  `math.max(sf.y, fullFrame.y + topInset)` en haut (robuste barre native affichée ou non).
- `~/.hammerspoon/init.lua` : `spoon.WindowSnap.topInset = 40` (hauteur SketchyBar).
- `~/.config/yabai/yabairc` (+ miroir `config/yabai/`) : `external_bar all:40:0` pour
  réserver le haut aussi en mode bsp (fenêtres tuilées). Appliqué à chaud.
- Vérifié : maximize → fenêtre à y=50 (40 barre + 10 gap), SketchyBar visible au-dessus.

**Fichiers :** `WindowSnap.spoon/init.lua`, `config/yabai/yabairc`, `~/.hammerspoon/init.lua`,
`~/.config/yabai/yabairc`.

**Commits :** voir ci-dessous (poussés GitLab + GitHub ; ~/.hammerspoon GitHub).

## 07:57:45 — Demande

> vas-y, applique la solution propre

## 11:26:35 — Demande

> il y a moyen de changer le cadre de la fenêtre suivant qu'elle est en "flottant" ou pas ?

## 11:40 — Réalisé : anneau de focus coloré selon l'état flottant/tuilé

**Demande :** changer le cadre (anneau JankyBorders) selon que la fenêtre est flottante ou non.

**Actions :**
- Constat : JankyBorders ne colore globalement que la fenêtre ACTIVE (pas de couleur
  par-fenêtre selon l'état). On reflète donc l'état de la seule fenêtre focalisée.
- `Yabai.spoon` : champs `borders` / `borderFloatColor` (ambre 0xffe0a13a) /
  `borderManagedColor` (bleu 0xff7aa2f7) ; `_refreshBorderColor()` (query is-floating →
  `borders active_color=…`) ; `startBorderSync()` (hook `windowFocused`). Refresh aussi
  après `toggle_float` et `layout_toggle` (états changés sans changement de focus).
- `~/.hammerspoon/init.lua` : `spoon.Yabai:startBorderSync()` après bindHotkeys.
- Validé : `borders` accepte la commande (exit 0) ; en float la fenêtre est is-floating →
  ambre ; bleu après passage bsp.

**Fichiers :** `Yabai.spoon/init.lua`, `~/.hammerspoon/init.lua`.

**Décisions / notes :** en mode `layout float` global, toutes les fenêtres sont flottantes
→ anneau toujours ambre ; le bleu n'apparaît qu'en bsp (⌃⌥⌘T) ou via toggle_float. Couleurs
choisies par l'utilisateur (ambre/bleu).

**Commits :** `8a7b634` (Yabai.spoon), `d25efea` (~/.hammerspoon) — poussés (GitLab + GitHub).

